
```python
list = ["hi","hello"]

# this will print Hi
index_0 = list[0].title() 
```


> The `title` method prints it in a more presentable way. 


- `append` : It appends the value at the end of the list.
- `insert(v,i)` : It describes itself.
- `del list[0]` : delete the first value.
- `pop` : removes the last item of the list.
- `pop(i)`
- `lis.remove(value)` : It removes only the first occurrence.  

The sort method changes the order of the list permanently.

```python
list.sort(reverse = true)
```

Sorting a list temporarily 

```python
# also accepts a reverse=ture
sorted(list)
```

- `reverse(list)`.
- `len(list)`.






